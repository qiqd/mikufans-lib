spring:
  profiles:
    active: dev
  application:
    name: mikufans-lib
  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb://root:z7c4vsgl@dbprovider.ap-northeast-1.clawcloudrun.com:31648/?directConnection=true
      database: mikufans-db
  jackson:
    time-zone: GMT+8
logging:
  charset:
    console: UTF-8
    file: UTF-8
  level:
    root: info
    org.mikufans: debug
    org.springframework.data.mongodb: debug
  file:
    name: ./log/mikufans-librarians-lib.log
  logback:
    rolling-policy:
      max-file-size: 5MB
server:
  port: 45279
